export interface Game{
    description: string[],
    features: string[],
    genre: string[],
    id: string,
    imgFolder: string,
    name: string;
    newGame: boolean,
    price: string
}